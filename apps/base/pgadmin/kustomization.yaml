apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  - pgadmin-config.yaml
  - pgadmin-deployment.yaml
  - ingress.yaml
# Note: pgadmin-credentials secret must be created manually via kubectl
# kubectl create secret generic pgadmin-credentials -n apps \
#   --from-literal=PGADMIN_DEFAULT_EMAIL="admin@homelab.local" \
#   --from-literal=PGADMIN_DEFAULT_PASSWORD="your-password"
