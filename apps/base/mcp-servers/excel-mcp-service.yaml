apiVersion: v1
kind: Service
metadata:
  name: excel-mcp
  namespace: mcp-servers
  labels:
    app: excel-mcp
spec:
  type: LoadBalancer
  selector:
    app: excel-mcp
  ports:
  - name: mcp
    port: 3000
    targetPort: 3000
    protocol: TCP
