apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  # MCP Gateway (Unla) - Central management UI
  - mcp-gateway-deployment.yaml
  - mcp-gateway-service.yaml
  - mcp-gateway-configmap.yaml
  - mcp-gateway-pvc.yaml
  # NOTE: Azure MCP Server removed - it only supports stdio transport (local CLI), 
  # not http server mode for K8s. Use it locally with VS Code/Claude instead.
  # Redis - For MCP server caching
  - redis-deployment.yaml
  - redisinsight-deployment.yaml
  # Ingress
  - ingress.yaml
