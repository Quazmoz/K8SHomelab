apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mcp-servers
resources:
  - namespace.yaml
  # MCP Gateway (Unla) - Central management UI
  - mcp-gateway-deployment.yaml
  - mcp-gateway-service.yaml
  - mcp-gateway-configmap.yaml
  - mcp-gateway-pvc.yaml
  # Azure MCP Server - DISABLED until Docker Hub images are built
  # Enable after running: ./build/build-and-push.sh
  # And creating: kubectl create secret docker-registry dockerhub-regcred ...
  #- azure-mcp-deployment.yaml
  #- azure-mcp-service.yaml
  #- azure-mcp-configmap.yaml
  # Excel MCP Server - DISABLED until ready
  #- excel-mcp-deployment.yaml
  #- excel-mcp-service.yaml
  #- excel-mcp-configmap.yaml
  # Ingress (Gateway routes only for now)
  - ingress.yaml
