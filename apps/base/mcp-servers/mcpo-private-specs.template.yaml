# Private OpenAPI Specs ConfigMap
# This file is a TEMPLATE - the actual specs are gitignored
# Create the real ConfigMap manually on your cluster using:
#   kubectl create configmap mcpo-private-specs -n apps \
#     --from-file=clickup-openapi.json=./private-specs/clickup-openapi.json
#
# Or apply this template after populating it locally
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcpo-private-specs
  namespace: apps
  labels:
    app: mcpo
data:
  # Specs are loaded from private-specs/ directory
  # This template shows the structure - actual content is gitignored
  placeholder.json: |
    {"info": {"title": "Placeholder - replace with real specs"}}
