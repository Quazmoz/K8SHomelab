apiVersion: v1
kind: Service
metadata:
  name: azure-mcp
  namespace: mcp-servers
  labels:
    app: azure-mcp
spec:
  type: LoadBalancer
  selector:
    app: azure-mcp
  ports:
  - name: mcp
    port: 3001
    targetPort: 3001
    protocol: TCP
