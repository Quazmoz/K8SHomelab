apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-backup-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: usb-slow-storage
  resources:
    requests:
      storage: 7Gi
  selector:
    matchLabels:
      type: usb-slow
