apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  - redis-deployment.yaml
  # - redisinsight-deployment.yaml # Temporarily disabled to free RAM
  # - ingress.yaml # RedisInsight ingress disabled with RedisInsight
  - redis-pdb.yaml
