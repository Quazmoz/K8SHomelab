---
# RedisInsight Deployment
# Official Redis GUI for browsing, querying, and monitoring
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redisinsight
  namespace: apps
  labels:
    app: redisinsight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redisinsight
  template:
    metadata:
      labels:
        app: redisinsight
    spec:
      nodeSelector:
        kubernetes.io/hostname: quinn-hpprobook430g6
      containers:
      - name: redisinsight
        image: redis/redisinsight@sha256:b5e19ee240abef6edb435871b90ff8a210995422e8e018ab61c0339d318a1f84
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5540
          name: web
        env:
        - name: RI_APP_PORT
          value: "5540"
        resources:
          requests:
            memory: "64Mi"
            cpu: "10m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 5540
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/health
            port: 5540
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
        - name: redisinsight-data
          mountPath: /data
      volumes:
      - name: redisinsight-data
        emptyDir: {}

---
# RedisInsight Service
apiVersion: v1
kind: Service
metadata:
  name: redisinsight
  namespace: apps
  labels:
    app: redisinsight
spec:
  type: ClusterIP
  ports:
  - port: 5540
    targetPort: 5540
    protocol: TCP
    name: web
  selector:
    app: redisinsight
