apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana
  namespace: apps
spec:
  interval: 5m
  chart:
    spec:
      chart: grafana
      version: "10.4.0"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
      interval: 1m
  values:
    # --- Sidecar for Dashboard Discovery ---
    sidecar:
      dashboards:
        enabled: true
        label: grafana_dashboard
        labelValue: "1"

    # --- Datasource Configuration ---
    datasources:
      datasources.yaml:
        apiVersion: 1
        datasources:
        - name: Prometheus
          type: prometheus
          uid: prometheus
          url: http://prometheus-server.apps.svc.cluster.local
          access: proxy
          isDefault: true
        # TODO: This datasource uses a password from an insecure, plain-text secret.
        # The secret should be encrypted with SOPS for any real-world use.
        - name: PostgreSQL
          type: postgres
          url: postgres-service:5432
          user: grafana_user
          database: grafana_db
          secureJsonData:
            password: "$__file{/etc/secrets/postgres-credentials/POSTGRES_PASSWORD}"
          jsonData:
            sslmode: "disable"
        # OpenWebUI database for audit queries
        - name: PostgreSQL-OpenWebUI
          type: postgres
          uid: PostgreSQL-OpenWebUI
          url: postgres-service:5432
          user: grafana_user
          database: openwebui_db
          secureJsonData:
            password: "$__file{/etc/secrets/postgres-credentials/POSTGRES_PASSWORD}"
          jsonData:
            sslmode: "disable"

    # --- Persistence ---
    persistence:
      enabled: true
      type: pvc
      storageClassName: "local-storage"
      size: 25Gi
      existingClaim: "grafana"

    # --- Secret Mounting ---
    # This securely mounts the postgres password into the Grafana pod
    extraSecretMounts:
      - name: postgres-credentials-file
        secretName: postgres-credentials
        mountPath: /etc/secrets/postgres-credentials
        readOnly: true

    # --- Resources (best-effort) ---
    resources:
      requests:
        memory: "64Mi"
        cpu: "10m"
      limits:
        cpu: 2000m
        memory: 2Gi
