apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Infrastructure
  - node-health.yaml
  - pod-resources.yaml
  - storage.yaml
  # Logs
  - loki-logs.yaml
  # Databases
  - postgresql.yaml
  - mongodb.yaml
  - redis.yaml
  # Applications
  - openwebui.yaml         # NEW: Consolidated from operations+audit+analytics
  - openwebui-advanced.yaml  # Resources dashboard (models, files, KB)
  - n8n-workflows.yaml
  - mcp-tools.yaml
  # Navigation
  - homelab-overview.yaml
