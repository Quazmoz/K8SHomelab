apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: apps
  labels:
    app: langfuse
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: langfuse
