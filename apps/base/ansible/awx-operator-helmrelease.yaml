apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: awx-operator
  namespace: apps
spec:
  interval: 5m
  chart:
    spec:
      chart: awx-operator
      version: "2.19.1"
      sourceRef:
        kind: HelmRepository
        name: awx-operator
        namespace: flux-system
      interval: 1m
  install:
    crds: Create
  upgrade:
    crds: Create
  values:
    watchNamespace: apps