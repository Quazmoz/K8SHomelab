---
# Docker MCP Gateway ConfigMap
# Configuration for MCP server catalog and settings
apiVersion: v1
kind: ConfigMap
metadata:
  name: docker-mcp-gateway-config
  namespace: apps
  labels:
    app: docker-mcp-gateway
data:
  # Docker MCP Gateway configuration
  # Add your MCP server catalog here
  docker-mcp.yaml: |
    # MCP Server Catalog Configuration
    # Define your MCP servers here
    # Example:
    # servers:
    #   - name: filesystem
    #     image: mcp/filesystem:latest
    #     description: "File system operations"
    #   - name: github
    #     image: mcp/github:latest
    #     description: "GitHub API access"
